<h2>Update room</h2>

<div class="row">
  <div class="span4 offset4">
    <%= form_for(@room) do |r| %>
      <%= r.label :name %>
      <%= r.text_field :name %>

      <%= r.submit "Save changes", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>